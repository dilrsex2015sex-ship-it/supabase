interface:
  display_name: "Supabase"
  short_description: "Repo-specific patterns and workflows for supabase"
  default_prompt: "Use the supabase repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true